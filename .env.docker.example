# Database Configuration
DB_HOST=postgres
DB_PORT=5432
DB_USER=ecar
DB_PASSWORD=change_this_password
DB_NAME=ecar_production

# Redis Configuration
REDIS_URL=redis://redis:6379/1

# Rails Configuration
RAILS_ENV=production
RAILS_SERVE_STATIC_FILES=true
RAILS_LOG_TO_STDOUT=true

# Security Keys - REPLACE THESE IN PRODUCTION!
SECRET_KEY_BASE=replace_with_generated_key
JWT_SECRET=replace_with_secure_random_string
DEVISE_SECRET_KEY=replace_with_another_secure_key

# Domain Configuration
API_DOMAIN=api.ecar.tn
ADMIN_DOMAIN=admin.ecar.tn

# CORS Settings
ALLOWED_ORIGINS=https://admin.ecar.tn,capacitor://localhost,http://localhost:3000

# SMTP Configuration for emails
SMTP_USERNAME=your_email@example.com
SMTP_PASSWORD=your_email_password
SMTP_DOMAIN=ecar.tn
SMTP_ADDRESS=smtp.example.com
SMTP_PORT=587

# API URL for frontend applications
API_URL=https://api.ecar.tn/api/v1 